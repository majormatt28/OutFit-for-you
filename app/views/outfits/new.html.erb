<h3>Let's Put Together A New Fit</h3>

<%= form_for @outfit do |f| %>
    <%= f.hidden_field :user_id, value: User.first.id %></p>
    <p><strong><%= f.label "Name:" %>
    <%= f.text_field :name %></p>
    <%= f.submit %>
<% end %>

<p><%= link_to "Go Back", outfits_path %>